import {Text, ScrollView, View, Image, TouchableOpacity} from 'react-native'
import {styles} from './style'
export const ScreenLogin = () =>{
    return(
       <ScrollView>
            <Text style={styles.ScrollView}></Text>
            <View style={styles.container}>
                <Image source={require('')}/>
            </View>
       </ScrollView>
    );
}