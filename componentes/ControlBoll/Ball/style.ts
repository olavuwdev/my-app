import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  container:{
    backgroundColor:'white',
    width:30,
    height:30,
    borderRadius:15,
    position:'absolute',
  }
})